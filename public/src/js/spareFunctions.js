(function( window, undefined ) {
    // define your function
    var footerTest = function () {
        alert('Footer Test works');
    }

    // Expose it to the global object
    window.footerTest = footerTest;
})(window);
